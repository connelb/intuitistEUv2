import { VideoScorePipe } from './video-score.pipe';

describe('VideoScorePipe', () => {
  it('create an instance', () => {
    const pipe = new VideoScorePipe();
    expect(pipe).toBeTruthy();
  });
});
