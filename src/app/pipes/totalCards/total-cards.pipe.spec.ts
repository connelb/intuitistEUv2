import { TotalCardsPipe } from './total-cards.pipe';

describe('TotalCardsPipe', () => {
  it('create an instance', () => {
    const pipe = new TotalCardsPipe();
    expect(pipe).toBeTruthy();
  });
});
