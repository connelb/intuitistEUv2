import { VideoScoreCardsPipe } from './video-score-cards.pipe';

describe('VideoScoreCardsPipe', () => {
  it('create an instance', () => {
    const pipe = new VideoScoreCardsPipe();
    expect(pipe).toBeTruthy();
  });
});
